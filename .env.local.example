# Token agents use to POST updates. Set the same value in Vercel env vars.
AGENT_API_TOKEN=change-me-to-a-secret

# Vercel KV — auto-injected when you add KV storage in the Vercel dashboard.
# Leave blank for local dev (falls back to data/dashboard.json).
KV_REST_API_URL=
KV_REST_API_TOKEN=
